<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">PairBnB</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Listing",listings_path %></li>
      </ul>
      <% if signed_out? and !current_page?(sign_in_path) %>
        <%= form_for :session, url: session_path, html: {class:"navbar-form navbar-right"} do |form| %>
          <div class="text-field form-group">
            <%= form.label :email %>
            <%= form.text_field :email, type: 'email', class:"form-control" %>
          </div>


          <div class="password-field form-group">
            <%= form.label :password %>
            <%= form.password_field :password, class:"form-control" %>
          </div>


          
            <%= form.submit class:"btn btn-success" %>
          <% end %>

          <% elsif current_page?(sign_in_path) %>
          <% else %>
            <ul class="nav navbar-nav">
              <li><%= link_to "My reservation", reservations_path  %></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <li>
                <div class="round">
                  <%= link_to current_user, class: "current-user" do %>
                    <%= image_tag current_user.profile_image_url(:thumb), title: "Your Profile" %>
                  <% end %>   
                </div>

              </li>
              <li><%= link_to "Sign Out", sign_out_path, method: :delete %></li>
            </ul>
             
             
        <% end %>


      </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>