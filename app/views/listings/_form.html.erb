<%= simple_form_for(@listing, html: { class: 'form-horizontal' }) do |f| %>
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :price, :label => "Price (in RM)" %>
	<%= f.input :tag_list, :label => "Tags (seperated with comma)" %>

	<%= f.button :submit %>
<% end %>
