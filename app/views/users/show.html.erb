<div class="container">
	<div class="row">
		<div class="col-md-2 col-md-offset-1">
			<%= image_tag @user.profile_image_url(:medium) %>
		</div>
		<div class="col-md-9">
			<h1><%= @user.firstname + " " + @user.lastname %></h1>
			<p><%= @user.email %></p>
			<%= link_to "Edit", edit_user_path, class: "btn btn-primary" if @user == current_user  %>
		</div>
	</div>
	

</div>